# Documentation Inconsistencies

This file tracks known inconsistencies between planning docs so they can be
resolved deliberately instead of getting lost during implementation.

When you find an inconsistency, add a section here with a short description
and a suggested resolution. Once the underlying docs are fixed, remove the
entry — git history preserves the audit trail.

_No open items._
